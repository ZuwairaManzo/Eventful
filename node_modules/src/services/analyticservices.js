import apiClient from '../utils/apiClient';

const getEventAnalytics = async (eventId) => {
  const response = await apiClient.get(`/analytics/${eventId}`);
  return response.data;
};

export default {
  getEventAnalytics,
};