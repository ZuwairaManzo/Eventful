import apiClient from '../utils/apiClient';

const shareEvent = async (eventId, platform) => {
  const response = await apiClient.post('/sharing/share', { eventId, platform });
  return response.data;
};

export default {
  shareEvent,
};