import apiClient from '../utils/apiClient';

const generateQRCode = async (ticketId) => {
  const response = await apiClient.post('/qrcodes/generate', { ticketId });
  return response.data.qrcode;
};

export default {
  generateQRCode,
};