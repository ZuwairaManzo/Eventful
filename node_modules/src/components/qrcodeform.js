import React from 'react';

const QRCodeViewer = ({ qrCode }) => {
  return (
    <div>
      <h2>QR Code</h2>
      <img src={`data:image/png;base64,${qrCode}`} alt="QR Code" />
    </div>
  );
};

export default QRCodeViewer;